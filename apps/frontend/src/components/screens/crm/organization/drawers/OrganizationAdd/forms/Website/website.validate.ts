import { WebsiteValidateProps } from ".";

export const websiteValidate = async (
	// eslint-disable-next-line @typescript-eslint/no-unused-vars
	{ form }: WebsiteValidateProps
): Promise<true | undefined> => {
	// const {} = form.values;
	return true;
}
