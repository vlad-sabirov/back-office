export enum NameDefaultEnum {
	'cancel' = 'Отмена',
	'prev' = 'Назад',
	'next' = 'Далее',
	'finish' = 'Завершить',
}

export enum ColorDefaultEnum {
	'cancel' = 'neutral',
	// eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
	'prev' = 'neutral',
	'next' = 'primary',
	'finish' = 'success',
}

export enum VariantDefaultEnum {
	'cancel' = 'easy',
	// eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
	'prev' = 'easy',
	'next' = 'hard',
	// eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
	'finish' = 'hard',
}
