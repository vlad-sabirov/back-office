export interface MutationReportRealizationRequest {
	year: number | string;
	month: number | string;
	plan: number | string;
	realization: number | string;
	customerCount: number | string;
	customerShipment: number | string;
	shipmentCount: number | string;
	userId: number | string;
}
