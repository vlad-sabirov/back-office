import { CrmReportRealization } from '@fsd/pages/crm-report-realization';
import { FC, Suspense } from 'react';

const Page: FC = () => (
	<Suspense>
		<CrmReportRealization />
	</Suspense>
);
export default Page;
