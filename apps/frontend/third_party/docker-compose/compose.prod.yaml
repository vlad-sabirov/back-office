version: '3'

services:
  frontend:
    build:
      context: ../../
      dockerfile: ./third_party/docker/prod.dockerfile
    restart: always
    command: npm run start
    volumes:
      - ../../public/:/app/public/
    env_file:
      - ../../../../env/.env.local
      - ../../../../env/.env.prod
