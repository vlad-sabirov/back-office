version: '3'

services:
   nginx:
     build:
       context: ../../
       dockerfile: ./third_party/docker/prod.dockerfile
     volumes:
       - ../../prod.nginx.conf:/etc/nginx/conf.d/default.conf
     restart: always
     env_file:
       - ../../../../env/.env.local
       - ../../../../env/.env.prod
