version: '3'

services:
   nginx:
     build:
       context: ../../
       dockerfile: ./third_party/docker/dev.dockerfile
     volumes:
       - ../../dev.nginx.conf:/etc/nginx/conf.d/default.conf
     restart: "no"
     env_file:
       - ../../../../env/.env.local
       - ../../../../env/.env.dev
