version: '3'

services:
   nginx:
      container_name: ${NGINX_CONTAINER_NAME}
      ports:
         - '${NGINX_PORT_HTTP}:80'
         - '${NGINX_PORT_HTTPS}:443'
      networks:
         - erp_network

