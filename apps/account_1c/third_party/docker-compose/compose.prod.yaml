version: '3'

services:
   account_1c:
      build:
         context: ../../
         dockerfile: ./third_party/docker/prod.dockerfile
      command: npm run start
      env_file:
         - ../../../../env/.env.local
         - ../../../../env/.env.prod
      restart: always

   db_account_1c:
      restart: always
      env_file:
            - ../../../../env/.env.local
